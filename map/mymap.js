var words = ["ground", "control", "to", "major", "tom"];

//my map function

function myMap (array, function) {
	newArray = [];
	return newArray;
}

map(words, function(word) {
  return word.length;
});